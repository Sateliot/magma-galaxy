apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgresql
  namespace: "{{ magma_namespace }}"
spec:
  template:
    spec:
      containers:
        - name: postgresql
          image: {{ postgresql_image }}

